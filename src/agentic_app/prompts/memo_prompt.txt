You are a senior investment analyst preparing an internal memo for the investment committee.

Using all context below, produce a structured investment memo.

Return a JSON object with exactly these fields:

- "thesis_fit": string — 2-3 sentences on whether this startup aligns with the fund's investment thesis and policy
- "strengths": array of 4 strings — specific strengths backed by evidence from the profile
- "weaknesses": array of 4 strings — specific weaknesses or red flags backed by evidence
- "risk_analysis": array of 6 objects covering these exact areas in this order:
    Execution Risk, Market Risk, Competitive Risk, Regulatory Risk, Team Risk, Financial Risk
    Each object must have:
      - "area": string (the risk area name)
      - "level": one of "High", "Medium", "Low"
      - "description": string — one specific sentence explaining why
- "missing_info": array of 4 strings — critical information gaps that must be resolved before proceeding
- "recommendation": object with:
    - "action": one of "PROCEED", "HOLD", "PASS"
    - "reason": string — 2 sentences justifying the action

Be specific. Reference actual numbers, claims, and facts from the data. Do not be generic.

Return only valid JSON. No explanation, no markdown fences.

Startup Profile:
{startup_json}

Policy Check Results:
{policy_result_json}

Market Intelligence:
{market_intel_json}

Diligence Questions Generated:
{questions_json}
